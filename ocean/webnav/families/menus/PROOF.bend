import Base
import ./LAWS.bend as L

def L.hovering_root_does_not_reveal_grandchild():
  {==}

def L.hovering_child_keeps_ancestor_expanded():
  {==}

def L.wait_is_identity(s):
  {==}

def L.invalid_zero_ref_is_ignored(task, count, nodes, open, hover, outcome):
  {==}

def L.expired_action_is_timeout(s, ref):
  {==}

def L.terminal_action_is_absorbed(task, count, nodes, open, hover, success, elapsed, deadline):
  {==}

def L.visible_root_menu_item(open, hover):
  {==}

def L.closed_second_menu_hides_items():
  {==}

def L.hovering_branch_reveals_child():
  {==}

def L.correct_leaf_scores_success():
  {==}

def L.disabled_leaf_scores_failure():
  {==}

def L.click_menu_branch_selection_fails():
  {==}

def L.opening_button_shows_menu():
  {==}
