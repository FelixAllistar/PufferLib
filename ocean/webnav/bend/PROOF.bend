import Base
import ./LAWS.bend as Laws

def Laws.terminal_absorbs(seed, task, target, order, checked, focus, text, length, tick, result, action):
  {==}

def Laws.observe_preserves_state(s, seed, action):
  {==}

def Laws.reset_discards_history(s, seed, action):
  {==}

def Laws.success_preserves_state(seed, task, target, order, checked, focus, text, length, tick, done, result):
  {==}

def Laws.failure_preserves_state(seed, task, target, order, checked, focus, text, length, tick, done, result):
  {==}

def Laws.unfocused_edit_is_identity(seed, task, target, order, checked, text, length, tick, done, result, action):
  {==}
