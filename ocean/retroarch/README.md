# retroarch (alias of ocean/retro)

Env folder is ocean/retro (config retro.ini). This alias exists because the request asked for ocean/retroarch.

Build/train:
  ./build.sh retro --gpu
  ./puffer train retro

See ocean/retro/README.md. Supply ROM via --env.rom_path if you have one.
